<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FAF9F6">

    <!-- THE LIVE SCANNER VIEW -->
    <com.journeyapps.barcodescanner.DecoratedBarcodeView
        android:id="@+id/barcode_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- INFO AND DISCONNECT UI -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:background="@android:color/white">

        <TextView
            android:id="@+id/scan_status_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Wheelchair Connection"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#1F2020"/>

        <TextView
            android:id="@+id/scan_status_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Align QR code within the frame to connect"
            android:layout_marginTop="4dp"
            android:textColor="#8C8C8C"/>
    </LinearLayout>
</LinearLayout>