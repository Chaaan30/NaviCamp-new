<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/rounded_dialog_background"
    android:padding="24dp">

    <!-- Lock icon circle -->
    <LinearLayout
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:background="@drawable/ic_lock_circle_bg"
        android:gravity="center">

        <ImageView
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_lock"
            app:tint="@color/onPrimaryColor"
            android:contentDescription="Lock icon" />
    </LinearLayout>

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="Confirm Your Password"
        android:fontFamily="@font/inter_bold"
        android:textColor="@color/primaryColor"
        android:textSize="20sp"
        android:layout_marginBottom="6dp" />

    <!-- Subtitle -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:gravity="center"
        android:text="Enter your password to authorize\nthis QR generation."
        android:fontFamily="@font/inter"
        android:textColor="@color/gray"
        android:textSize="13sp"
        android:layout_marginBottom="24dp" />

    <!-- Password field -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/qr_password_input_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:hint="Password"
        app:startIconDrawable="@drawable/ic_lock"
        app:startIconTint="@color/primaryColor"
        app:endIconMode="password_toggle"
        app:endIconTint="@color/primaryColor"
        app:boxStrokeColor="@color/primaryColor"
        app:hintTextColor="@color/primaryColor"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/qr_password_input"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/inter"
            android:inputType="textPassword"
            android:singleLine="true"
            android:textSize="15sp" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Buttons row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <Button
            android:id="@+id/qr_password_cancel_btn"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:layout_marginEnd="8dp"
            android:text="Cancel"
            android:fontFamily="@font/inter_bold"
            android:textSize="14sp"
            android:textColor="@color/primaryColor"
            android:background="@drawable/button_secondary_ripple"
            android:paddingHorizontal="20dp" />

        <Button
            android:id="@+id/qr_password_authorize_btn"
            android:layout_width="wrap_content"
            android:layout_height="44dp"
            android:text="Authorize"
            android:fontFamily="@font/inter_bold"
            android:textSize="14sp"
            android:textColor="@color/onPrimaryColor"
            android:background="@drawable/button_primary_ripple"
            android:paddingHorizontal="20dp" />
    </LinearLayout>

</LinearLayout>
